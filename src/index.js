import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';




ReactDOM.render(
  <div>
    <App/>
  </div>,
  document.getElementById('root')
);



//create a new React App
//create a app.jsx component
//create a header.jsx component that render the <header> element
//to show the keeper app name in <h1></h1>
//create a footer.jsx component that returns footer element
//to show copyright message in a <p> with dynamically updated year
//create a note.jsx to show div element with a h1 for title and p for content




